// FormEditor.tsx
import React from 'react'

interface FormEditorProps {
  formId: string
  initialData: Record<string, string>
}

const FormEditor: React.FC<FormEditorProps> = ({ formId, initialData }) => {
  return (
    <div>
      {/* Form editor implementation */}
    </div>
  )
}

export default FormEditor
